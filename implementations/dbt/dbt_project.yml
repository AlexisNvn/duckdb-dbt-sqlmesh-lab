name: local_analytics
version: '1.0.0'
config-version: 2
profile: local_analytics
model-paths: [models]
test-paths: [tests]
macro-paths: [macros]
models:
  local_analytics:
    +materialized: table
    +persist_docs:
      relation: true
      columns: true
